---
layout: base.njk
title: Archive
permalink: /posts/
---
{% from "post-card.njk" import card %}

<section class="wrap tag-cloud-section">
  <h1>Archive</h1>
  <div class="tag-cloud category-cloud">
    {% for cat in collections.categoryCloud %}
      <a class="tag-cloud-item" href="{{ cat.url }}">{{ cat.title }}
        <span class="tag-cloud-count">({{ cat.count }})</span></a>
    {% endfor %}
  </div>
  <div class="tag-cloud">
    {% for tagInfo in collections.tagCloud %}
      <a class="tag-cloud-item weight-{{ tagInfo.weight }}" href="/tags/{{ tagInfo.tag | slugify }}/">{{ tagInfo.tag }}
        <span class="tag-cloud-count">({{ tagInfo.count }})</span></a>
    {% else %}
      <p>No tags yet.</p>
    {% endfor %}
  </div>
</section>

<section class="wrap">
  <div class="post-grid">
    {% for post in collections.posts %}
      {{ card(post) }}
    {% endfor %}
  </div>
</section>